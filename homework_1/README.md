To run the code, install the packages and read the CSV file in the first cell. \
Then, initialize the functions by running the cell titled (functions). \
Lastly, the cells under the analysis section can be run sequentially, descending by task. \
  Task 3 outputs 
      43428 rows removed
      88885 rows remaining
  Task 4 outputs
      NUMBER_OF_SEATS      -0.260883
      CAPACITY_IN_POUNDS    2.001876
      dtype: float64
      as well as 4 histograms
  Task 5 outputs
      2 bar charts showing the proportions
  Task 6 outputs
      the size of the model dataset
      a table of the RMSEs for each model and target
