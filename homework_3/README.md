To run this code, you must connect to an EC2 instance capable of running what PySpark requires. \\

It also assumes that the data required has been downloaded into the EC2 instance.\\

Then, the code can just be run sequentially.
